on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 'ff1'
      run: ls
      
    - uses: actions/checkout@v3

    - name: 'ff2'
      run: ls
   
   
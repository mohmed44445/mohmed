on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    
    - uses: actions/checkout@v3

    - name: 'login'
      run: docker login --username mohmednnnn --password dckr_pat_IknGPWEFWBrwaEgEkzvLeISH-uQ
   
   
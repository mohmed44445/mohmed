on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 'ff1'
      run: ls

    - uses: action/checkout@v3

    - name: 'ff2'
      run: ls
   
   